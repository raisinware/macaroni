CFLAGS="-march=bonnell -mno-cx16 --param=l1-cache-line-size=64 --param=l1-cache-size=24 --param=l2-cache-size=512 -mfunction-return=keep -mindirect-branch=keep -mharden-sls=none" -O2 -pipe"
CXXFLAGS="${CFLAGS}"
FFLAGS="${CFLAGS}"
FCFLAGS="${CFLAGS}"